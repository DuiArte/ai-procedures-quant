# Procedure · <NAME>

**Goal:** <one sentence>

**Inputs:** <data / files, as placeholders — never real private paths>

**Preconditions / gates:**
- [ ] <gate 1>
- [ ] <gate 2>

## Steps

1. <step> — `<command or action with `<placeholder>` params>`
2. <step>
3. <step>

## Outputs

- <artifact> → `<destination>`

## Failure handling

| Failure | Detection | Action |
|---|---|---|
| <mode> | <signal> | <response> |

> Author note: keep every parameter a `<placeholder>`. If a real value must appear to
> make the procedure usable, that value is private — leave the placeholder and
> document the real value in the private copy only.
